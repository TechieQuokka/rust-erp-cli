-- Enable UUID extension for better UUID handling
-- Version: 007
-- Description: Enable UUID extension for proper UUID support

-- Enable UUID extension if not already enabled
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";